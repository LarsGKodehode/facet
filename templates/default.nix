{
  nodejs = {
    description = "A NodeJS template with PNPM set as the package manager";
    path = ./nodejs;
  };
  csharp = {
    description = "A NodeJS template with PNPM set as the package manager";
    path = ./csharp;
  };
}
